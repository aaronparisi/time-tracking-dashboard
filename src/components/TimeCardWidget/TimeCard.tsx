import React, { ReactElement } from "react";

interface TimeCardProps {}

const TimeCard: React.FC<TimeCardProps> = (): ReactElement => {
	return <div className="time-card">TC</div>;
};

export default TimeCard;
